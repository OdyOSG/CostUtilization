% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CalculateCostOfCare.R
\name{calculateCostOfCare}
\alias{calculateCostOfCare}
\title{Calculate Cost of Care Analysis}
\usage{
calculateCostOfCare(
  connection = NULL,
  connectionDetails = NULL,
  cdmDatabaseSchema,
  cohortDatabaseSchema,
  cohortTable,
  cohortId,
  costOfCareSettings,
  aggregated = TRUE,
  tempEmulationSchema = NULL,
  verbose = TRUE
)
}
\arguments{
\item{connection}{A live \code{DatabaseConnector} or \code{DBI} connection object.}

\item{connectionDetails}{Optional \code{ConnectionDetails} to create a connection
if \code{connection} is not supplied (provide exactly one of the two).}

\item{cdmDatabaseSchema}{Schema (or database) that contains CDM tables.}

\item{cohortDatabaseSchema}{Schema (or database) that contains the cohort table.}

\item{cohortTable}{Name of the cohort table.}

\item{cohortId}{Numeric cohort definition id to analyze.}

\item{costOfCareSettings}{A settings object from \code{createCostOfCareSettings()}.}

\item{aggregated}{Should aggregate statistics be computed instead of covariates per cohort entry? If aggregated is set to FALSE, the results returned will be based on each subject_id and cohort_start_date in your cohort table. If your cohort contains multiple entries for the same subject_id (due to different cohort_start_date values), you must carefully set the rowIdField so you can identify the patients properly.}

\item{tempEmulationSchema}{Optional schema for temp table emulation (Oracle/Redshift/...).}

\item{verbose}{Logical; print progress messages.}
}
\value{
A list with two tibbles: \code{results} and \code{diagnostics}.
}
\description{
Runs the cost-of-care analysis for a specified cohort using a validated
\code{CostOfCareSettings} object. Creates temp helper tables as needed
(visit restrictions, event filters, CPI) and executes the SQL plan.
}
