% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporalCostSettings.R
\name{costTemporalCovariateSettings}
\alias{costTemporalCovariateSettings}
\title{Create temporal covariate settings for cost analysis}
\usage{
costTemporalCovariateSettings(
  useCostInWindow = TRUE,
  temporalStartDays = c(-365, -180, -30, 0),
  temporalEndDays = c(-1, -1, -1, 0),
  costDomainIds = NULL,
  costConceptIds = NULL,
  costTypeConceptIds = NULL,
  currencyConceptIds = NULL,
  aggregateMethod = "sum",
  includeCostConceptId = FALSE,
  includeCostTypeConceptId = FALSE,
  includeCostDomainId = TRUE,
  covariateIdOffset = 1000
)
}
\arguments{
\item{useCostInWindow}{Logical indicating whether to extract costs within time windows}

\item{temporalStartDays}{A vector of integers specifying the start of time windows
(days relative to cohort start date). Negative values indicate days before.}

\item{temporalEndDays}{A vector of integers specifying the end of time windows
(days relative to cohort start date). Must be same length as temporalStartDays.}

\item{costDomainIds}{A vector of strings specifying which cost domains to include
(e.g., "Drug", "Procedure", "Visit"). If NULL, includes all domains.}

\item{costConceptIds}{A vector of concept IDs to filter specific cost concepts.
If NULL, includes all cost concepts.}

\item{costTypeConceptIds}{A vector of concept IDs specifying cost types to include
(e.g., allowed amount, paid amount). If NULL, includes all types.}

\item{currencyConceptIds}{A vector of concept IDs for specific currencies.
If NULL, includes all currencies.}

\item{aggregateMethod}{Method for aggregating costs: "sum", "mean", "median", "min", "max"}

\item{includeCostConceptId}{Logical indicating whether to create separate covariates
for each cost concept ID}

\item{includeCostTypeConceptId}{Logical indicating whether to create separate covariates
for each cost type concept ID}

\item{includeCostDomainId}{Logical indicating whether to create separate covariates
for each cost domain}

\item{covariateIdOffset}{An integer offset for generating unique covariate IDs}

\item{includeRevenueCodes}{Logical indicating whether to include revenue code analysis}

\item{includeDrgCodes}{Logical indicating whether to include DRG code analysis}
}
\value{
An object of type \code{costTemporalCovariateSettings}, to be used with other
functions in the CostUtilization package.
}
\description{
Creates settings for extracting cost-based temporal covariates. These settings
can be used to analyze costs within specific time windows relative to cohort
start dates.
}
